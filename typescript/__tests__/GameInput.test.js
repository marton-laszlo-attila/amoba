import GameInput from "../src/components/GameInput";

function sum(a, b) { return a + b; }

test('Simple test, adds 1 + 2 to equal 3', () => {
  expect(sum(1, 2)).toBe(3);
});

describe('Testing GameInput', () => {
  it("Add Brand at the /api/brands", async () => {

  });
})